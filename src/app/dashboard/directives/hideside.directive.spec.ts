import { HidesideDirective } from './hideside.directive';

describe('HidesideDirective', () => {
  it('should create an instance', () => {
    const directive = new HidesideDirective();
    expect(directive).toBeTruthy();
  });
});
