import { MinimenuDirective } from './minimenu.directive';

describe('MinimenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MinimenuDirective();
    expect(directive).toBeTruthy();
  });
});
