import { DisableDivDirective } from './disable-div.directive';

describe('DisableDivDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDivDirective();
    expect(directive).toBeTruthy();
  });
});
